<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SAFI Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">SAFI Blog</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="btn btn-outline-success" href="/new">Create New Post</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <header class="bg-primary text-white text-center py-5">
        <div class="container">
            <h1>Welcome to SAFI Blog</h1>
            <p class="lead">Explore insightful articles and share your thoughts.</p>
        </div>
    </header>

    <div class="container my-5">
        <h2 class="mb-4">Recent Posts</h2>
        <% if (posts.length > 0) { %>
            <div class="row g-4">
                <% posts.forEach(post => { %>
                    <div class="col-md-6 col-lg-4">
                        <div class="card h-100 shadow-sm">
                            <div class="card-body d-flex flex-column">
                                <h5 class="card-title text-primary"><%= post.title %></h5>
                                <p class="card-text flex-grow-1"><%= post.content %></p>
                                <div class="mt-3 d-flex justify-content-between">
                                    <a href="/edit/<%= post.id %>" class="btn btn-sm btn-outline-primary">Edit</a>
                                    <form action="/delete/<%= post.id %>" method="POST">
                                        <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="alert alert-info text-center">
                <p>No posts available. <a href="/new" class="alert-link">Create your first post!</a></p>
            </div>
        <% } %>
    </div>

    <footer class="bg-light text-center py-4">
        <p class="mb-0">Made with ❤️‍🔥 in 🇲🇦.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
